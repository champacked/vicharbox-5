// src/config.ts
export const BACKEND_URL = `${import.meta.env.VITE_BACKEND_URL}` as string; // Replace with your actual backend URL

export const CLOUDINARY_URL = `${
  import.meta.env.VITE_CLOUDINARY_URL
}` as string;
